export default function Hero() {
  return (
    <section className="pt-4 pb-4">
      <p className="text-sm font-semibold uppercase tracking-[0.25em] text-cyan-700">
        FLOWFORGE
      </p>

      <h1 className="mt-4 max-w-5xl text-6xl font-bold tracking-tight text-slate-900">
        Convert plain-English operational requests into validated, editable, and executable workflows.
      </h1>

      <p className="mt-6 max-w-3xl text-xl leading-8 text-slate-600">
        FlowForge compiles business processes into structured workflow steps, checks policy and dependencies, and lets teams review, edit, and export the final plan.
      </p>
    </section>
  );
}